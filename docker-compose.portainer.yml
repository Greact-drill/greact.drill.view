services:
  view-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_URL
    expose:
      - '80'
    environment:
      - NODE_ENV=production
      - VITE_API_URL=${VITE_API_URL}
    networks:
      - proxy

networks:
  proxy:
    external: true